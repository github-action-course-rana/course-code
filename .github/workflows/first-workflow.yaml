name: first-workflow
on: [push]

jobs:
    run-shell-commands:
        runs-on: ubuntu-latest
        steps:
            - name: Print a string
              run: echo "Hello World from Github Actions"
            - name: Multiline Commands
              run: |
                node -v
                npm -v 